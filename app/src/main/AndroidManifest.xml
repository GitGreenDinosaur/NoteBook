<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xinhai.notebook">
    <!-- 指纹权限 -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!--控制手机震动-->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".NoteBookApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.记事本">
        <activity
            android:name=".ui.activity.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.PrivateActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.WasterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.PrePrivateActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.EditActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>